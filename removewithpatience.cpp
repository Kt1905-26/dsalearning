//replace string r with prev char in s when r is the substring of s
#include<bits/stdc++.h>
using namespace std;
int main(){
    char s,r;
    cin>>s;
    cin>>r;
    int n=r.length();
    int d=s.find(r);
    while(d!+string::npos){
        if(d>0{
            char prev=s[d-1];
        char replacement(n,c);
        S.replace(d,n,replacement);
        d=S.find(r,d+n);
      
       



        }
        else{
            s=s.substring(n);
            d=s.find(r);


        }
         
        


    }
    cout<<s<<endl;


}